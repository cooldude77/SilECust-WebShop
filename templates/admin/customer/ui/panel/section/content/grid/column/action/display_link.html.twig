{% set after =  path(listGrid.config.display_link.redirect_upon_success_route) %}
{% set href =  path(listGrid.config.display_link.route,{'id':id}) %}
    <div class="d-flex flex mb-1 ml-0 ">
        <a class="link-primary" id="{{ link_id ~"-display-"~id}}" href="{{ href }}">
        {{ name }}
    </a>
</div>