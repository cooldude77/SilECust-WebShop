{# {% block content %}
    <div class="row-flex mr-auto">

    </div>
    <div id="addresses">
        <h2>{{ addressTypeCaption }} <a class="btn btn-link" href="{{ path('sc_my_addresses') }}">
                <i class="bi bi-geo-alt-fill"></i>Manage Addresses
            </a></h2>

        {{ form_start(form) }}
        <ul class="address">
            {% for address in form.addresses %}
                <li>{{ form_widget(address) }}</li>
                {{ address.vars.value.id| singleLine }}
            {% endfor %}
        </ul>
        {{ form_end(form) }}
    </div>
{% endblock %}
#}
{#
NOTE: How to use a variable as array key ( see constant below)
#}
{{ form(form) }}
<div id="">
    {% set constant = constant('\\Silecust\\WebShop\\Service\\Component\\Routing\\RoutingConstants::REDIRECT_UPON_SUCCESS_URL') %}
    <a href="{{ path('sc_web_shop_checkout_address_create',
        {
            'type':request.query.get("type"),
            (constant): path('sc_web_shop_checkout_addresses')

        }) }}">Or create new ... </a>
</div>